# Dev guide
required node: v24

1. add `.env` file following the `.env_example`

2. replace `<app_id>` with your https://openweathermap.org/ key

3. run:
    ```console
    npm install
    npm run dev
    ````

4. enjoy the app
